{
  "name": "the-great-escape",
  "version": "1.0.0",
  "description": "The Great Escape - Full build (server + client)",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "express": "^4.18.2",
    "fs-extra": "^11.1.1",
    "socket.io": "^4.7.2"
  },
  "engines": {
    "node": ">=16"
  }
}
